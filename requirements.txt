# Inference (H200). Pin bumps go with a note in RUNBOOK.md.
transformers>=4.57
accelerate>=1.0
vllm>=0.11
torch>=2.6
huggingface_hub>=0.26

# Scoring and analysis (runs fine on CPU)
jsonschema>=4.20
pandas>=2.0
matplotlib>=3.8
